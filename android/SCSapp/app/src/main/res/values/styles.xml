<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/sigmaColorCyan</item>
        <item name="colorPrimaryDark">@color/sigmaColorCyan</item>
        <item name="colorAccent">@color/sigmaColorCyan</item>
        <item name="android:colorBackground">@color/wallet_bright_foreground_holo_dark</item>
        <item name="android:navigationBarColor">@color/sigmaColorCyan</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/black</item>
        <item name="android:textColorSecondaryInverse">@android:color/black</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="AppThemeCyan" parent="AppTheme">
        <item name="android:colorForeground">
            @color/cast_expanded_controller_ad_label_background_color
        </item>
        <item name="android:colorBackground">@color/common_google_signin_btn_text_light_disabled
        </item>
    </style>

</resources>
